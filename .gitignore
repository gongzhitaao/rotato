# Concrete deploy config — contains the bootstrap BWS token and secret UUIDs.
# Only the *.example template is committed.
deploy/*.env
!deploy/*.env.example

# Python
.venv/
__pycache__/
*.egg-info/
.pytest_cache/
